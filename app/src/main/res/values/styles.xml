<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="cardTheme">
        <item name="android:textColorHint">@android:color/black</item>//hint color And Label Color in False State
        <item name="colorAccent">@color/colorAccent</item> //Label color in True State And Bar Color False And True State
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorControlActivated">@color/btncolor</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@color/colorAccent</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/drawable</item>
    </style>

    <style name="CustomShowcaseTheme2" parent="ShowcaseView">
        <!--<item name="sv_backgroundColor">#80000000</item>-->
        <item name="sv_backgroundColor">#212121</item>
        <item name="sv_showcaseColor">#003366</item>
        <item name="sv_buttonText">Got It</item>
        <item name="sv_titleTextAppearance">@style/CustomTitle2</item>
        <item name="sv_detailTextAppearance">@style/CustomText2</item>
    </style>

    <style name="CustomTitle2" parent="TextAppearance.ShowcaseView.Title.Light">
        <item name="android:textColor">#deffffff</item>
    </style>

    <style name="CustomText2" parent="TextAppearance.ShowcaseView.Detail.Light">
        <item name="android:textColor">#8affffff</item>
    </style>

</resources>
